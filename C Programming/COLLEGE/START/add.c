// WAP TO ADD TWO NUMBERS
#include <stdio.h>
int main()
{
    int x, y, sum = 0;
    printf("ENTER THE VALUE: ");
    scanf("%d%d", &x, &y);
    sum = x + y;
    printf("sum=%d", sum);
    return 0;
}
