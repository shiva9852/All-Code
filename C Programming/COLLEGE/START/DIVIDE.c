// WAP TO PERFORM DIVISION BETWEEN TWO NUMBERS
#include <stdio.h>
int main()
{
    int x, y, D;
    printf("ENTER THE NUMBERS= ");
    scanf("%d%d", &x, &y);
    D = x / y;
    printf("QUOTIENT =%d", D);
    return 0;
}