// WAP to display odd numbers from 2 to 30
#include <stdio.h>
int main()
{
    int n,s=0;
    // printf("Enter a number = ");
    // scanf("%d",&n);
    for (int i = 100; i <=455; i++)
    {

        s = s + 1;
    }
    printf("%d", s);
    return 0;
}